import React from "react"; // Reactライブラリをインポート

function App() { // Appという関数コンポーネントを定義
  return (
    <div> // divタグを返す
      <h1>Hello World</h1> // h1タグで "Hello World" を表示
    </div>
  );
}

export default App; // Appコンポーネントをエクスポートし、他のファイルで使用可能にする